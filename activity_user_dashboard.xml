<!-- activity_user_dashboard.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@drawable/ic_main">

    <TextView
        android:id="@+id/tvWelcomeMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome To Cattle Health. We are dedicated to provide quality reliable services for your cattle"
        android:textSize="18sp"
        android:textColor="@android:color/black"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:gravity="center"/>

    <Button
        android:id="@+id/btnAccessVetServices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvWelcomeMessage"
        android:layout_marginTop="16dp"
        android:text="Access Veterinary Services" />

    <Button
        android:id="@+id/btnHealthRecords"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnAccessVetServices"
        android:layout_marginTop="16dp"
        android:text="Health Records and Reports" />

    <Button
        android:id="@+id/btnChatForum"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnHealthRecords"
        android:layout_marginTop="16dp"
        android:text="Chat and Update Forum" />

    <Button
        android:id="@+id/btnAccessAIServices"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnChatForum"
        android:layout_marginTop="16dp"
        android:text="Access A.I Services" />

    <Button
        android:id="@+id/btnMakePayment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnAccessAIServices"
        android:layout_marginTop="16dp"
        android:text="Make payment" />
</RelativeLayout>
